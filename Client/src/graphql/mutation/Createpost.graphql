mutation createpost($message: String!, $roomId: Int!) {
    createpost(roomId: $roomId, message: $message) {
        post {
            ...RegularPosts
        }
        errors {
            ...RegularError
        }
        success {
            ...RegularSuccess
        }
    }
}
