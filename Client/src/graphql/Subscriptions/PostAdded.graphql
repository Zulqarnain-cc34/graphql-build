subscription getNewPost($roomId: Int!) {
    Postadded(roomId: $roomId) {
        post {
            ...RegularPosts
        }
        errors {
            ...RegularError
        }
        success {
            ...RegularSuccess
        }
    }
}
